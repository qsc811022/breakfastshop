# Change Log

## 2026-06-19

### Modified Files

- `index.html`
- `styles.css`

### Changes

- Replaced the hero image source with `assets/logo.jpg`.
- Removed the hero title, eyebrow text, and description text from the first screen.
- Kept only three hero action buttons:
  - `查看菜單`
  - `線上點餐`
  - `Google Maps`
- Moved the hero buttons upward on desktop so they do not sit too close to the bottom edge.
- Improved the mobile layout:
  - Made the mobile header more compact.
  - Changed the mobile hero image to show the full logo/banner instead of cropping it.
  - Moved the three hero buttons below the image on mobile.
  - Added a smaller-button layout for very narrow screens under `380px`.

### Reason

The original hero section duplicated text that was already included in the banner image and looked crowded. On mobile, the fixed-height hero and cropped image made the layout hard to read. The updated layout keeps the banner visible and turns the first screen into a cleaner quick-action area.
